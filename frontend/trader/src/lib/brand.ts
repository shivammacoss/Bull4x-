/** Dashboard / in-app product name (landing & marketing match Bull4x). */
export const BRAND_NAME = 'Bull4x';

/** Zustand persist key for UI preferences (theme, terminal layout). */
export const STORAGE_KEY_UI = 'bull4x-ui';

/** Legacy key — migrated once on load; logout clears legacy auth key. */
export const STORAGE_KEY_UI_LEGACY = 'trustedge-ui';
